# setup.cfg

[metadata]
# Can duplicate some info from pyproject.toml, but primarily for older tools.
# name, version etc. are usually taken from pyproject.toml if present.

[options]
package_dir =
    = src
packages = find:
include_package_data = True

[options.packages.find]
where = src